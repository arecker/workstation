---
- hosts: local
  roles:
    - base

  tasks:
    - name: add emacs ppa
      apt_repository: repo="ppa:kelleyk/emacs"
      become: true
    - name: install package
      package: name="emacs" state="present"
      become: true
